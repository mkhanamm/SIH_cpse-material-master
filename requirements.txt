# --- core ---
pandas==3.0.2
numpy==2.4.4
openpyxl==3.1.5
scikit-learn==1.8.0
scipy==1.17.1
networkx==3.6.1
joblib==1.5.3

# --- string similarity (string/attribute channel) ---
rapidfuzz==3.14.3
jellyfish==1.3.0

# --- semantic channel ---
# Production backend (config.SEMANTIC_BACKEND = "sbert").
# Requires a one-time model download; the repo falls back to the offline
# "tfidf_svd" backend automatically if these are absent or unreachable.
sentence-transformers==3.3.1
torch==2.5.1

# --- app + dashboard ---
streamlit==1.63.0
plotly==6.3.1
pydantic==2.12.4

# --- tests ---
pytest==8.4.2
